// Placeholder stub — this feature isn't part of the BotBuilder extraction yet.
// TODO: implement or port from the main site if/when this tab is needed.
const Analysistool = () => (
    <div style={{ padding: '48px', textAlign: 'center', color: 'var(--text-general, #999)' }}>
        <h3>Analysis Tool</h3>
        <p>This feature isn't available in this build yet.</p>
    </div>
);

export default Analysistool;
